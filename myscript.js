var mainCtrl = function($scope) {
    $scope.addUser = function() {
        // 省略
    }
    
    $scope.colors = [
        'red',
        'blue',
        'pink'
    ];
    
    $scope.initColor = $scope.colors[2];
}
